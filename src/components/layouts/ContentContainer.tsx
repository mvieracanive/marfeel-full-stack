import { Box, styled } from "@mui/material"

const StyledBox = styled(Box)({
    display: 'flex',
    flexDirection: 'column',
    gap: '10px',
    margin: '10px 20%'
})

export const ContentContainer = StyledBox
